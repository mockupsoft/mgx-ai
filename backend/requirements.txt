# Core Application Dependencies
# Install with: pip install -r requirements.txt

# FastAPI & Web Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0
alembic>=1.12.0

# Redis (for distributed caching)
redis>=5.0.0

# MetaGPT & Core Agent
metagpt>=0.8.0
tenacity>=8.2.0

# Async & Concurrency
httpx>=0.24.0,<0.29.0

# Google Gemini (DeepSite / LLM routing) — resmi google-genai SDK
google-genai>=1.0.0
asyncio-contextmanager>=1.0.0

# WebSocket & Events
broadcaster>=0.2.0
python-multipart>=0.0.6

# Git / GitHub
PyGithub>=2.5.0
GitPython>=3.1.40

# Utilities
pyyaml>=6.0

# Observability
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-sqlalchemy>=0.46b0
opentelemetry-instrumentation-httpx>=0.46b0
langsmith>=0.1.0

# Structured Logging
structlog>=23.0.0  # Structured logging with JSON support

# Authentication & Security
python-jose[cryptography]>=3.3.0  # JWT token handling
passlib[bcrypt]>=1.7.4  # Password hashing with bcrypt
email-validator>=2.0.0  # Email validation for Pydantic

# LLM SDK - pin to versions compatible with httpx>=0.24
anthropic>=0.40.0
openai>=1.52.0

# Type hints
types-PyYAML>=6.0.0

# DeepSite preview (screenshot endpoint) — tarayıcı: `playwright install chromium`
playwright>=1.42.0

# Development/Testing tools are in requirements-dev.txt
