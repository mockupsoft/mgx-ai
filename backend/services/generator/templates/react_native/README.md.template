# {{PROJECT_NAME}}

React Native (TypeScript) template generated by MGX.

## Prerequisites

- Node.js 20+
- Android Studio / Xcode for device builds

## Commands

- `npm install` — install dependencies
- `npx react-native start` — Metro bundler
- `npx react-native run-android` / `run-ios` — run on device

## Environment

Set `API_BASE_URL` for your backend API.
