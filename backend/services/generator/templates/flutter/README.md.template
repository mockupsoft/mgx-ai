# {{PROJECT_NAME}}

Flutter template generated by MGX.

## Setup

1. Rename `name:` in `pubspec.yaml` if needed (lowercase, underscores).
2. `flutter pub get`
3. `flutter run`

## Environment

Configure `API_BASE_URL` in your app settings when connecting to a backend.
