# {{PROJECT_NAME}}

Go + [Fiber](https://gofiber.io/) API template generated by MGX.

## Run locally

```bash
go mod tidy
go run cmd/main.go
```

Health check: `GET http://localhost:{{PORT}}/health`

## Docker

```bash
docker build -t {{PROJECT_NAME}} .
docker run -p {{PORT}}:{{PORT}} --env-file .env {{PROJECT_NAME}}
```
