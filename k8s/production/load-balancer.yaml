apiVersion: v1
kind: Service
metadata:
  name: mgx-agent-lb
  namespace: production
  labels:
    app: mgx-agent
spec:
  type: LoadBalancer
  selector:
    app: mgx-agent
    color: blue
  ports:
    - name: http
      port: 80
      targetPort: 8000
